# Superseded mirror (archived, not deleted)

This tree was the ecosystem-side mirror of the desktop docs (D3 naming
canon, 2026-08-13 morning). It violated the one-topic-one-home rule: the
content's canonical home is the component repo itself —
**[shesh-desktop/docs/SHESH/](https://github.com/gaganjainse/shesh-desktop/tree/main/docs/SHESH)** —
and two writable copies had already provably diverged (`SheshaMemory`
renamed here, not there). Retired the same day per the documentation
policy's SSOT table. The shesh-docs book mirrors Part IV straight from the
desktop repo (`tools/book_build.py`); nothing consumes this tree anymore.
